# Optional: enables LLM summarization of digests
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Path to YAML config (default: ./competitors.yaml)
COMPETITORPULSE_CONFIG=./competitors.yaml

# Data directory for snapshots & digests (default: ./data)
COMPETITORPULSE_DATA=./data
